<?php

declare(strict_types=1);

namespace Watson\Symfony;

use Symfony\Component\HttpKernel\Bundle\Bundle;

/**
 * Auto-registered as a `symfony-bundle` (composer.json `type`). Symfony
 * Flex picks it up on `composer require --dev watson/symfony` and adds it
 * to `config/bundles.php` automatically.
 */
final class WatsonBundle extends Bundle
{
}
