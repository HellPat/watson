<?php

declare(strict_types=1);

// Laravel artisan stub — file's presence is what watson uses to detect Laravel.
// Real artisan boots the framework; this fixture never runs it, so the body
// is intentionally minimal.

require_once __DIR__.'/laravel-stubs.php';

echo "watson laravel-micro fixture\n";
