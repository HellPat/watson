<?php

declare(strict_types=1);

// Minimal bootstrap to make the fixture look like a Laravel 11 app.
// watson does not execute this; it only parses sources for entry points.

return static function () {
    // no-op
};
