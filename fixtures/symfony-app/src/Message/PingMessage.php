<?php

declare(strict_types=1);

namespace App\Message;

/**
 * Fixture message — used by the Behat scenario that asserts watson surfaces
 * `symfony.message_handler` entries via `debug:container --tag=messenger.message_handler`.
 */
final class PingMessage
{
}
