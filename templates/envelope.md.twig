{# Top-level markdown wrapper. Iterates analyses + includes per-analysis partials. #}
# watson — {{ language }}

_tool {{ tool }} v{{ version }}_

{% if context.base is defined and context.head is defined %}
Diff: `{{ short_rev(context.base) }}` → `{{ short_rev(context.head) }}`
{% endif %}
Root: `{{ context.root }}`

{% if sources is not empty %}
{% include '_sources.md.twig' %}
{% endif %}
{% for analysis in analyses %}
## {{ analysis.name }}
_v{{ analysis.version }}_

{% if not analysis.ok %}
**Error**: {{ analysis.error.message|default('(unknown)') }}

{% else %}
{% include analysis.name ~ '.md.twig' %}

{% endif %}
{% endfor %}
